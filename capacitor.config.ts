import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ngx-entended-pdf-viewer-compatibility-with-ion-router-outlet',
  webDir: 'www'
};

export default config;
